import {
  notification,
  success,
  failure,
  verification,
} from "../util/embedFunctions";
import fetch from "node-fetch";
import e from "express";

module.exports = {
  name: "ban",
  description: "Bans the user.",
  async execute(msg, args) {
    
  },
};
